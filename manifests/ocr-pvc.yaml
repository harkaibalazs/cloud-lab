apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ocr-models
  namespace: cloudlab
spec:
  storageClassName: nfs-client
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
